#lobbies
  h1
    | Welcome to WebCiv, {{ username }}!
  p
    button(ng-click='openModal("newlobby")') New Lobby
  p(ng-repeat='lobby in lobbies')
    | {{ lobby.name }} -
    button.lobbyJoin(ng-click='lobbyJoin(lobby.name)')
      | Join
